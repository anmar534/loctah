# 🏗️ خطة إعادة الهيكلة المعمارية (Refactoring Plan)

## 📋 نظرة عامة

هذا المستند يوضح خطة إعادة هيكلة المشروع بناءً على مبدأ **Single Responsibility Principle (SRP)** وفصل الطبقات (Separation of Concerns).

### المشكلة الحالية:
- الصفحات تحتوي على 4-5 طبقات مختلطة (Data Fetching + Business Logic + Form Management + UI)
- صعوبة الاختبار والصيانة
- تكرار الكود في أماكن متعددة
- صعوبة إعادة استخدام المنطق

### الحل المقترح:
فصل كل طبقة إلى مجلدات وملفات مخصصة مع استخدام **index.ts** للتصدير المركزي.

---

## 🎯 الطبقات المعمارية

```
┌─────────────────────────────────────────────────┐
│  1. Pages Layer (Routing Only)                 │
│     ↓                                           │
│  2. Components Layer (UI Only)                  │
│     ↓                                           │
│  3. Hooks Layer (State + Side Effects)          │
│     ↓                                           │
│  4. Services Layer (Business Logic)             │
│     ↓                                           │
│  5. API Layer (Data Fetching)                   │
└─────────────────────────────────────────────────┘
```

---

## 📁 الهيكل التفصيلي

### 1️⃣ Pages Layer - طبقة التوجيه

```
app/[locale]/(admin)/admin/
├── stores/
│   ├── page.tsx                    ✅ موجود - يحتاج إعادة هيكلة
│   │   الوظيفة: Wrapper فقط، يستدعي <StoresList />
│   │   الحالة: يحتوي حالياً على منطق fetching + UI (يجب فصلهما)
│   │
│   ├── new/
│   │   └── page.tsx                ✅ موجود - يحتاج إعادة هيكلة
│   │       الوظيفة: Wrapper فقط، يستدعي <StoreForm mode="create" />
│   │       الحالة: يحتوي حالياً على 320 سطر (Form + Logic + UI)
│   │
│   └── [id]/
│       └── edit/
│           └── page.tsx            ✅ موجود - يحتاج إعادة هيكلة
│               الوظيفة: Wrapper فقط، يستدعي <StoreForm mode="edit" id={params.id} />
│               الحالة: يحتوي على منطق معقد يجب فصله
│
├── products/
│   ├── page.tsx                    ✅ موجود - يحتاج إعادة هيكلة
│   ├── create/
│   │   └── page.tsx                ✅ موجود - يحتاج إعادة هيكلة (337 سطر)
│   └── [id]/
│       └── edit/
│           └── page.tsx            ✅ موجود - يحتاج إعادة هيكلة
│
├── offers/
│   ├── page.tsx                    ✅ موجود - يحتاج إعادة هيكلة
│   ├── create/
│   │   └── page.tsx                ✅ موجود - يحتاج إعادة هيكلة (356 سطر)
│   └── [id]/
│       └── edit/
│           └── page.tsx            ✅ موجود - يحتاج إعادة هيكلة
│
├── categories/
│   ├── page.tsx                    ✅ موجود
│   ├── create/
│   │   └── page.tsx                ✅ موجود
│   └── [id]/
│       └── edit/
│           └── page.tsx            ✅ موجود
│
└── users/
    └── page.tsx                    ✅ موجود
```

**مثال على Page بعد إعادة الهيكلة:**
```tsx
// ✅ app/[locale]/(admin)/admin/stores/new/page.tsx (15 سطر فقط)
import { StoreForm } from '@/components/admin/stores';

export default function NewStorePage() {
  return <StoreForm mode="create" />;
}
```

---

### 2️⃣ Components Layer - طبقة الواجهة (UI فقط)

```
components/
├── admin/                          ✅ موجود
│   ├── DataTable.tsx               ✅ موجود - لا يحتاج تعديل
│   ├── ImageUpload.tsx             ✅ موجود - لا يحتاج تعديل
│   ├── ConfirmDialog.tsx           ✅ موجود - لا يحتاج تعديل
│   ├── SearchableSelect.tsx        ✅ موجود - لا يحتاج تعديل
│   ├── CategoryTreeItem.tsx        ✅ موجود - لا يحتاج تعديل
│   ├── PageHeader.tsx              ✅ موجود - لا يحتاج تعديل
│   ├── EmptyState.tsx              ✅ موجود - لا يحتاج تعديل
│   ├── LoadingTable.tsx            ✅ موجود - لا يحتاج تعديل
│   ├── StatsCard.tsx               ✅ موجود - لا يحتاج تعديل
│   ├── StatusBadge.tsx             ✅ موجود - لا يحتاج تعديل
│   ├── DynamicSpecs.tsx            ✅ موجود - لا يحتاج تعديل
│   │
│   ├── stores/                     ❌ غير موجود - يجب إنشاؤه
│   │   ├── StoresList.tsx          ❌ جديد
│   │   │   الوظيفة: عرض قائمة المتاجر مع DataTable
│   │   │   الاعتماد: يستخدم useStores hook
│   │   │   المسؤولية: UI فقط (عرض البيانات، لا منطق)
│   │   │
│   │   ├── StoreForm.tsx           ❌ جديد
│   │   │   الوظيفة: نموذج إضافة/تعديل متجر
│   │   │   الاعتماد: يستخدم useStoreForm hook
│   │   │   المسؤولية: عرض الحقول فقط، المنطق في الـ hook
│   │   │
│   │   ├── StoreCard.tsx           ❌ جديد
│   │   │   الوظيفة: بطاقة عرض متجر واحد
│   │   │   المسؤولية: UI فقط (تنسيق العرض)
│   │   │
│   │   ├── StoreFilters.tsx        ❌ جديد
│   │   │   الوظيفة: فلاتر البحث (مدينة، حالة...)
│   │   │   المسؤولية: UI فقط، التصفية في الـ hook
│   │   │
│   │   └── index.ts                ❌ جديد
│   │       الوظيفة: تصدير مركزي لكل مكونات المتاجر
│   │       المحتوى: export { StoresList, StoreForm, StoreCard, StoreFilters };
│   │
│   ├── products/                   ❌ غير موجود - يجب إنشاؤه
│   │   ├── ProductsList.tsx        ❌ جديد
│   │   │   الوظيفة: عرض قائمة المنتجات
│   │   │   الاعتماد: يستخدم useProducts hook
│   │   │
│   │   ├── ProductForm.tsx         ❌ جديد
│   │   │   الوظيفة: نموذج إضافة/تعديل منتج
│   │   │   الاعتماد: يستخدم useProductForm hook
│   │   │   المسؤولية: عرض الحقول (صور، تصنيفات، مواصفات)
│   │   │
│   │   ├── ProductCard.tsx         ❌ جديد
│   │   │   الوظيفة: بطاقة عرض منتج واحد
│   │   │
│   │   ├── ProductFilters.tsx      ❌ جديد
│   │   │   الوظيفة: فلاتر البحث (تصنيف، براند، سعر)
│   │   │
│   │   ├── ProductImages.tsx       ❌ جديد
│   │   │   الوظيفة: إدارة رفع وعرض صور المنتج
│   │   │   المسؤولية: UI فقط، منطق الرفع في ImageUpload
│   │   │
│   │   └── index.ts                ❌ جديد
│   │
│   ├── offers/                     ❌ غير موجود - يجب إنشاؤه
│   │   ├── OffersList.tsx          ❌ جديد
│   │   │   الوظيفة: عرض قائمة العروض
│   │   │   الاعتماد: يستخدم useOffers hook
│   │   │
│   │   ├── OfferForm.tsx           ❌ جديد
│   │   │   الوظيفة: نموذج إضافة/تعديل عرض
│   │   │   الاعتماد: يستخدم useOfferForm hook
│   │   │
│   │   ├── OfferCard.tsx           ❌ جديد
│   │   │   الوظيفة: بطاقة عرض عرض واحد
│   │   │
│   │   ├── DiscountCalculator.tsx  ❌ جديد
│   │   │   الوظيفة: واجهة حساب الخصم (عرض النتيجة فقط)
│   │   │   الاعتماد: يستخدم useDiscountCalculator hook
│   │   │   المسؤولية: عرض حقلي السعر + نسبة الخصم المحسوبة
│   │   │
│   │   ├── OfferDatePicker.tsx     ❌ جديد
│   │   │   الوظيفة: اختيار تواريخ بداية ونهاية العرض
│   │   │
│   │   └── index.ts                ❌ جديد
│   │
│   └── categories/                 ❌ غير موجود - يجب إنشاؤه
│       ├── CategoriesList.tsx      ❌ جديد
│       │   الوظيفة: عرض التصنيفات في جدول شجري
│       │
│       ├── CategoryForm.tsx        ❌ جديد
│       │   الوظيفة: نموذج إضافة/تعديل تصنيف
│       │
│       ├── CategoryTree.tsx        ❌ جديد
│       │   الوظيفة: عرض شجرة التصنيفات الهرمية
│       │   الاعتماد: يستخدم CategoryTreeItem الموجود
│       │
│       ├── CategorySelector.tsx    ❌ جديد
│       │   الوظيفة: اختيار تصنيف من قائمة
│       │
│       └── index.ts                ❌ جديد
│
├── layout/                         ✅ موجود
│   ├── AdminHeader.tsx             ✅ موجود - لا يحتاج تعديل
│   ├── AdminSidebar.tsx            ✅ موجود - لا يحتاج تعديل
│   ├── Footer.tsx                  ✅ موجود
│   ├── Header.tsx                  ✅ موجود
│   ├── MobileNav.tsx               ✅ موجود
│   ├── UserNav.tsx                 ✅ موجود
│   └── index.ts                    ❌ جديد - للتصدير المركزي
│
└── ui/                             ✅ موجود (shadcn/ui)
    └── ... (21 مكون)              ✅ لا يحتاج تعديل
```

---

### 3️⃣ Hooks Layer - طبقة الحالة والـ Side Effects

```
lib/hooks/
├── useAuth.ts                      ✅ موجود - لا يحتاج تعديل
├── useDebounce.ts                  ✅ موجود - لا يحتاج تعديل
├── useCategories.ts                ✅ موجود - لا يحتاج تعديل
├── useFavorites.ts                 ✅ موجود - لا يحتاج تعديل
├── useOffers.ts                    ✅ موجود - لا يحتاج تعديل
├── useProduct.ts                   ✅ موجود - لا يحتاج تعديل
├── useProducts.ts                  ✅ موجود - لا يحتاج تعديل
├── useStore.ts                     ✅ موجود - لا يحتاج تعديل
├── useStores.ts                    ✅ موجود - لا يحتاج تعديل
│
├── useStoreForm.ts                 ❌ جديد - مطلوب
│   الوظيفة: إدارة حالة نموذج المتجر (create/edit)
│   المدخلات: initialStore?: Store
│   المخرجات: { form, logoUrl, isSubmitting, handleSubmit }
│   الاعتماد: يستخدم react-hook-form + storeSchema + storesService
│   المنطق: 
│     - إعداد النموذج مع القيم الافتراضية
│     - معالجة الإرسال (تحويل البيانات ثم استدعاء API)
│     - إدارة حالة التحميل والأخطاء
│
├── useProductForm.ts               ❌ جديد - مطلوب
│   الوظيفة: إدارة حالة نموذج المنتج
│   المخرجات: { form, images, specifications, isSubmitting, handleSubmit }
│   الاعتماد: يستخدم productTransformer + productsService
│   المنطق:
│     - إدارة الصور المتعددة
│     - إدارة المواصفات الديناميكية
│     - تحويل البيانات قبل الإرسال
│
├── useOfferForm.ts                 ❌ جديد - مطلوب
│   الوظيفة: إدارة حالة نموذج العرض
│   المخرجات: { form, calculatedDiscount, isSubmitting, handleSubmit }
│   الاعتماد: يستخدم useDiscountCalculator + offersService
│   المنطق:
│     - ربط حساب الخصم تلقائياً
│     - تحويل التواريخ إلى ISO 8601
│
├── useCategoryForm.ts              ❌ جديد - مطلوب
│   الوظيفة: إدارة حالة نموذج التصنيف
│   المخرجات: { form, parentCategories, isSubmitting, handleSubmit }
│   الاعتماد: يستخدم categoriesService
│
├── useDiscountCalculator.ts       ❌ جديد - مطلوب
│   الوظيفة: حساب نسبة الخصم تلقائياً
│   المدخلات: originalPrice, discountedPrice
│   المخرجات: { discount, isValid }
│   الاعتماد: يستهلك discountService.calculateDiscount()
│   ⚠️ مهم: لا يكرر المنطق، فقط يستدعي الـ service
│
├── usePagination.ts                ❌ جديد - مطلوب
│   الوظيفة: إدارة حالة الصفحات (Pagination)
│   المخرجات: { currentPage, totalPages, goToPage, nextPage, prevPage }
│   المنطق: عام وقابل لإعادة الاستخدام
│
├── useTableActions.ts              ❌ جديد - مطلوب
│   الوظيفة: إدارة إجراءات الجداول (حذف، تفعيل/تعطيل)
│   المخرجات: { handleDelete, handleToggleStatus, isLoading }
│   الاعتماد: يستخدم useConfirm
│
├── useConfirm.ts                   ❌ جديد - مطلوب
│   الوظيفة: إدارة مربعات التأكيد
│   المخرجات: { confirm, isOpen, closeDialog }
│   الاستخدام: const { confirm } = useConfirm();
│              await confirm({ title, message });
│
├── useFetch.ts                     ❌ جديد - مطلوب
│   الوظيفة: Hook عام لاستدعاء API مع حالات loading/error
│   المدخلات: apiFunction
│   المخرجات: { data, loading, error, refetch }
│   الاستخدام: const { data, loading } = useFetch(() => listStores());
│
├── useFormPersist.ts               ❌ جديد - مطلوب (اختياري)
│   الوظيفة: حفظ بيانات النموذج في localStorage
│   المدخلات: formKey, formValues
│   الاستخدام: حفظ بيانات النموذج عند الكتابة واستعادتها عند العودة
│
└── index.ts                        ❌ جديد - للتصدير المركزي
    المحتوى: export * from './useAuth';
             export * from './useStoreForm';
             ... إلخ
```

---

### 4️⃣ Services Layer - طبقة منطق الأعمال

```
lib/services/                       ❌ غير موجود - يجب إنشاؤه
├── stores.service.ts               ❌ جديد
│   الوظيفة: منطق الأعمال الخاص بالمتاجر
│   الدوال:
│     - validateStoreData(store): التحقق من صحة البيانات
│     - prepareStoreForSubmit(formData, logoUrl): تجهيز البيانات للإرسال
│     - canDeleteStore(store): فحص إمكانية الحذف
│   ⚠️ لا يحتوي على API calls، فقط منطق الأعمال
│
├── products.service.ts             ❌ جديد
│   الوظيفة: منطق الأعمال الخاص بالمنتجات
│   الدوال:
│     - validateProductImages(images): التحقق من الصور
│     - prepareSpecifications(specs): تحويل المواصفات
│     - calculateProductScore(product): حساب تقييم المنتج
│
├── offers.service.ts               ❌ جديد
│   الوظيفة: منطق الأعمال الخاص بالعروض
│   الدوال:
│     - validateOfferDates(start, end): التحقق من التواريخ
│     - isOfferActive(offer): فحص نشاط العرض
│     - prepareOfferForSubmit(formData): تجهيز بيانات العرض
│
├── categories.service.ts           ❌ جديد
│   الوظيفة: منطق الأعمال الخاص بالتصنيفات
│   الدوال:
│     - buildCategoryTree(categories): بناء الشجرة الهرمية
│     - canDeleteCategory(category): فحص إمكانية الحذف
│     - getCategoryPath(categoryId): الحصول على المسار الكامل
│
├── discount.service.ts             ❌ جديد
│   الوظيفة: حساب الخصومات
│   الدوال:
│     - calculateDiscount(original, discounted): حساب نسبة الخصم
│     - calculateDiscountedPrice(original, percent): حساب السعر بعد الخصم
│     - validateDiscount(discount): التحقق من صحة الخصم
│   مثال:
│     ```ts
│     export function calculateDiscount(
│       originalPrice: number,
│       discountedPrice: number
│     ): number | null {
│       if (!originalPrice || !discountedPrice) return null;
│       if (discountedPrice >= originalPrice) return null;
│       return Math.round(((originalPrice - discountedPrice) / originalPrice) * 100);
│     }
│     ```
│
├── validation.service.ts           ❌ جديد
│   الوظيفة: تحققات مخصصة إضافية
│   الدوال:
│     - validateSlugUniqueness(slug): التحقق من عدم تكرار الـ slug
│     - validateImageFormat(file): التحقق من صيغة الصورة
│     - validateDateRange(start, end): التحقق من نطاق التواريخ
│
└── index.ts                        ❌ جديد
    المحتوى: export * from './stores.service';
             export * from './products.service';
             ... إلخ
```

---

### 5️⃣ Utils Layer - الأدوات المساعدة

```
lib/utils/
├── slugify.ts                      ✅ موجود - لا يحتاج تعديل
├── format.ts                       ✅ موجود - لا يحتاج تعديل
│
├── formatters/                     ❌ غير موجود - يجب إنشاؤه
│   ├── price.formatter.ts          ❌ جديد
│   │   الوظيفة: تنسيق الأسعار
│   │   الدوال:
│   │     - formatPrice(value, locale, currency): تنسيق عملة
│   │     - formatPriceRange(min, max): تنسيق نطاق سعر
│   │
│   ├── date.formatter.ts           ❌ جديد
│   │   الوظيفة: تنسيق التواريخ
│   │   الدوال:
│   │     - formatDate(date, format): تنسيق تاريخ
│   │     - formatRelativeTime(date): تنسيق نسبي (منذ ساعتين)
│   │     - formatDateRange(start, end): تنسيق نطاق تاريخ
│   │
│   ├── number.formatter.ts         ❌ جديد
│   │   الوظيفة: تنسيق الأرقام
│   │   الدوال:
│   │     - formatNumber(value): تنسيق رقم (1,000)
│   │     - formatCompact(value): تنسيق مختصر (1K, 1M)
│   │
│   ├── text.formatter.ts           ❌ جديد
│   │   الوظيفة: تنسيق النصوص
│   │   الدوال:
│   │     - truncate(text, length): اختصار نص
│   │     - capitalize(text): حرف كبير أول
│   │
│   └── index.ts                    ❌ جديد
│       المحتوى: export * from './price.formatter';
│                ... إلخ
│
├── transformers/                   ❌ غير موجود - يجب إنشاؤه
│   ├── product.transformer.ts      ❌ جديد
│   │   الوظيفة: تحويل بيانات المنتجات
│   │   الدوال:
│   │     - transformProductFormData(formData, images, specs): تحويل من النموذج للـ API
│   │     - transformProductResponse(apiData): تحويل من API للعرض
│   │   مثال:
│   │     ```ts
│   │     export function transformProductFormData(
│   │       formData: ProductFormData,
│   │       images: string[],
│   │       specifications: Specification[]
│   │     ) {
│   │       const attributes: Record<string, string> = {};
│   │       specifications.forEach((spec) => {
│   │         if (spec.key && spec.value) {
│   │           attributes[spec.key] = spec.value;
│   │         }
│   │       });
│   │       
│   │       return {
│   │         ...formData,
│   │         images: images.map(url => ({ url, alt: formData.title })),
│   │         image: images[0],
│   │         attributes: Object.keys(attributes).length > 0 ? attributes : undefined,
│   │       };
│   │     }
│   │     ```
│   │
│   ├── offer.transformer.ts        ❌ جديد
│   │   الوظيفة: تحويل بيانات العروض
│   │   الدوال:
│   │     - transformOfferFormData(formData): تحويل التواريخ إلى ISO
│   │     - transformOfferResponse(apiData): تحويل للعرض
│   │
│   ├── store.transformer.ts        ❌ جديد
│   │   الوظيفة: تحويل بيانات المتاجر
│   │   الدوال:
│   │     - transformStoreFormData(formData, logoUrl): تحويل بيانات المتجر
│   │     - transformStoreResponse(apiData): تحويل للعرض
│   │
│   ├── category.transformer.ts     ❌ جديد
│   │   الوظيفة: تحويل بيانات التصنيفات
│   │   الدوال:
│   │     - transformCategoryFormData(formData): تحويل بيانات التصنيف
│   │     - flattenCategoryTree(tree): تسطيح الشجرة الهرمية
│   │
│   └── index.ts                    ❌ جديد
│
└── validators/                     ❌ غير موجود - يجب إنشاؤه
    ├── image.validator.ts          ❌ جديد
    │   الوظيفة: التحقق من الصور
    │   الدوال:
    │     - validateImageSize(file): فحص حجم الصورة
    │     - validateImageFormat(file): فحص صيغة الصورة
    │     - validateImageDimensions(file): فحص أبعاد الصورة
    │
    ├── slug.validator.ts           ❌ جديد
    │   الوظيفة: التحقق من الـ slug
    │   الدوال:
    │     - isValidSlug(slug): فحص صحة الـ slug
    │     - slugExists(slug): فحص تكرار الـ slug
    │
    ├── date.validator.ts           ❌ جديد
    │   الوظيفة: التحقق من التواريخ
    │   الدوال:
    │     - isValidDateRange(start, end): فحص نطاق التاريخ
    │     - isFutureDate(date): فحص إذا كان التاريخ مستقبلي
    │
    └── index.ts                    ❌ جديد
```

---

### 6️⃣ API Layer - طبقة جلب البيانات

```
lib/api/
├── admin.ts                        ✅ موجود - لا يحتاج تعديل
├── auth.ts                         ✅ موجود - لا يحتاج تعديل
├── categories.ts                   ✅ موجود - لا يحتاج تعديل
├── client.ts                       ✅ موجود - لا يحتاج تعديل
├── favorites.ts                    ✅ موجود - لا يحتاج تعديل
├── offers.ts                       ✅ موجود - لا يحتاج تعديل
├── products.ts                     ✅ موجود - لا يحتاج تعديل
├── stores.ts                       ✅ موجود - لا يحتاج تعديل
├── vendor.ts                       ✅ موجود - لا يحتاج تعديل
└── index.ts                        ❌ جديد - للتصدير المركزي
```

---

### 7️⃣ Validations Layer - طبقة التحقق

```
lib/validations/
├── store.ts                        ✅ موجود - لا يحتاج تعديل
├── category.ts                     ✅ موجود - لا يحتاج تعديل
├── product.ts                      ✅ موجود - لا يحتاج تعديل
├── offer.ts                        ✅ موجود - لا يحتاج تعديل
└── index.ts                        ❌ جديد - للتصدير المركزي
```

---

### 8️⃣ Tests Layer - طبقة الاختبارات

```
tests/                              ❌ غير موجود - يجب إنشاؤه بالكامل
├── __mocks__/                      ❌ جديد
│   ├── api.mock.ts                 ❌ جديد
│   │   الوظيفة: محاكاة استجابات API
│   │   المحتوى: بيانات تجريبية للمتاجر، المنتجات، العروض
│   │
│   ├── data.mock.ts                ❌ جديد
│   │   الوظيفة: بيانات تجريبية للاختبار
│   │   المحتوى: mockStore, mockProduct, mockOffer
│   │
│   └── handlers.mock.ts            ❌ جديد
│       الوظيفة: MSW handlers لاعتراض الطلبات
│       الاستخدام: Mock Service Worker
│
├── components/                     ❌ جديد
│   ├── admin/
│   │   ├── DataTable.test.tsx      ❌ جديد
│   │   │   الاختبارات:
│   │   │     - يعرض البيانات بشكل صحيح
│   │   │     - الفرز يعمل بشكل صحيح
│   │   │     - الفلترة تعمل بشكل صحيح
│   │   │     - الـ pagination يعمل بشكل صحيح
│   │   │
│   │   ├── ImageUpload.test.tsx    ❌ جديد
│   │   │   الاختبارات:
│   │   │     - يرفع الصورة بنجاح
│   │   │     - يعرض رسالة خطأ للصيغ غير المدعومة
│   │   │     - يحذف الصورة بنجاح
│   │   │
│   │   ├── ConfirmDialog.test.tsx  ❌ جديد
│   │   │   الاختبارات:
│   │   │     - يفتح المربع عند الاستدعاء
│   │   │     - يغلق عند الإلغاء
│   │   │     - ينفذ الإجراء عند التأكيد
│   │   │
│   │   └── DynamicSpecs.test.tsx   ❌ جديد
│   │       الاختبارات:
│   │         - يضيف مواصفة جديدة
│   │         - يحذف مواصفة موجودة
│   │         - يحدّث قيمة مواصفة
│   │
│   ├── stores/
│   │   ├── StoresList.test.tsx     ❌ جديد
│   │   ├── StoreForm.test.tsx      ❌ جديد
│   │   └── StoreCard.test.tsx      ❌ جديد
│   │
│   ├── products/
│   │   ├── ProductsList.test.tsx   ❌ جديد
│   │   ├── ProductForm.test.tsx    ❌ جديد
│   │   └── ProductCard.test.tsx    ❌ جديد
│   │
│   ├── offers/
│   │   ├── OffersList.test.tsx     ❌ جديد
│   │   ├── OfferForm.test.tsx      ❌ جديد
│   │   └── DiscountCalculator.test.tsx ❌ جديد
│   │
│   └── categories/
│       ├── CategoriesList.test.tsx ❌ جديد
│       ├── CategoryForm.test.tsx   ❌ جديد
│       └── CategoryTree.test.tsx   ❌ جديد
│
├── hooks/                          ❌ جديد
│   ├── useStoreForm.test.ts        ❌ جديد
│   │   الاختبارات:
│   │     - يحمّل البيانات الأولية بشكل صحيح
│   │     - يرسل البيانات بنجاح
│   │     - يعرض أخطاء التحقق
│   │
│   ├── useProductForm.test.ts      ❌ جديد
│   ├── useOfferForm.test.ts        ❌ جديد
│   ├── useDiscountCalculator.test.ts ❌ جديد
│   │   الاختبارات:
│   │     - يحسب الخصم بشكل صحيح
│   │     - يرجع null للقيم غير الصحيحة
│   │     - يتعامل مع الحالات الحدية (0, سالب...)
│   │
│   ├── usePagination.test.ts       ❌ جديد
│   ├── useConfirm.test.ts          ❌ جديد
│   └── useFetch.test.ts            ❌ جديد
│
├── services/                       ❌ جديد
│   ├── stores.service.test.ts      ❌ جديد
│   ├── products.service.test.ts    ❌ جديد
│   ├── offers.service.test.ts      ❌ جديد
│   ├── discount.service.test.ts    ❌ جديد
│   │   الاختبارات:
│   │     - calculateDiscount(100, 80) → 20
│   │     - calculateDiscount(100, 100) → null
│   │     - calculateDiscount(100, 120) → null
│   │
│   └── validation.service.test.ts  ❌ جديد
│
├── utils/                          ❌ جديد
│   ├── formatters/
│   │   ├── price.formatter.test.ts ❌ جديد
│   │   ├── date.formatter.test.ts  ❌ جديد
│   │   └── number.formatter.test.ts ❌ جديد
│   │
│   ├── transformers/
│   │   ├── product.transformer.test.ts ❌ جديد
│   │   ├── offer.transformer.test.ts ❌ جديد
│   │   └── store.transformer.test.ts ❌ جديد
│   │
│   ├── validators/
│   │   ├── image.validator.test.ts ❌ جديد
│   │   └── slug.validator.test.ts  ❌ جديد
│   │
│   ├── slugify.test.ts             ❌ جديد
│   └── format.test.ts              ❌ جديد
│
├── e2e/                            ❌ جديد (اختياري - للمستقبل)
│   ├── stores.e2e.test.ts          ❌ جديد
│   │   السيناريوهات:
│   │     - إنشاء متجر جديد بنجاح
│   │     - تعديل متجر موجود
│   │     - حذف متجر
│   │     - البحث والفلترة
│   │
│   ├── products.e2e.test.ts        ❌ جديد
│   ├── offers.e2e.test.ts          ❌ جديد
│   └── categories.e2e.test.ts      ❌ جديد
│
└── setup/                          ❌ جديد
    ├── jest.setup.ts               ❌ جديد
    │   الوظيفة: إعدادات Jest الأساسية
    │   المحتوى: import '@testing-library/jest-dom';
    │
    ├── test-utils.tsx              ❌ جديد
    │   الوظيفة: دوال مساعدة للاختبار
    │   المحتوى: renderWithProviders, mockRouter
    │
    └── msw.setup.ts                ❌ جديد
        الوظيفة: إعداد Mock Service Worker
        الاستخدام: اعتراض طلبات API أثناء الاختبار
```

---

## 🔄 أمثلة على التدفق بعد إعادة الهيكلة

### مثال 1: إنشاء متجر جديد

```
User Action
    ↓
Page Wrapper (15 سطر)
    ↓
StoreForm Component (UI فقط - 80 سطر)
    ↓
useStoreForm Hook (State + Side Effects - 100 سطر)
    ↓
storesService (Business Logic - 50 سطر)
    ↓
storeTransformer (Data Transformation - 30 سطر)
    ↓
API Layer (createStore - موجود)
```

### مثال 2: حساب الخصم في العروض

```
User Types Prices
    ↓
OfferForm Component
    ↓
useOfferForm Hook
    ↓
useDiscountCalculator Hook
    ↓
discountService.calculateDiscount() (Business Logic)
    ↓
Returns calculated discount
    ↓
Display in UI
```

---

## ⚠️ نقاط مهمة يجب الانتباه لها

### 1. تجنب التكرار بين Hooks و Services

```tsx
// ❌ خطأ: تكرار المنطق
// useDiscountCalculator.ts
const discount = Math.round(((original - discounted) / original) * 100);

// discount.service.ts
const discount = Math.round(((original - discounted) / original) * 100);

// ✅ صحيح: الـ Hook يستهلك الـ Service
// useDiscountCalculator.ts
import { calculateDiscount } from '@/lib/services/discount.service';

export function useDiscountCalculator(original, discounted) {
  const [discount, setDiscount] = useState<number | null>(null);
  
  useEffect(() => {
    const result = calculateDiscount(original, discounted);
    setDiscount(result);
  }, [original, discounted]);
  
  return { discount, isValid: discount !== null };
}
```

### 2. استخدام index.ts للاستيراد المركزي

```tsx
// ❌ قبل: استيرادات متعددة
import { StoresList } from '@/components/admin/stores/StoresList';
import { StoreForm } from '@/components/admin/stores/StoreForm';
import { StoreCard } from '@/components/admin/stores/StoreCard';

// ✅ بعد: استيراد واحد
import { StoresList, StoreForm, StoreCard } from '@/components/admin/stores';
```

### 3. Component Factory للتكرار المستقبلي (اختياري)

إذا كان هناك تشابه كبير بين الصفحات، يمكن إنشاء مصنع:

```tsx
// lib/factories/createListPage.tsx
export function createListPage<T>({
  useDataHook,
  ListComponent,
  pageTitle,
}: {
  useDataHook: () => { data: T[], loading: boolean };
  ListComponent: React.ComponentType<{ data: T[] }>;
  pageTitle: string;
}) {
  return function ListPage() {
    const { data, loading } = useDataHook();
    
    return (
      <>
        <PageHeader title={pageTitle} />
        {loading ? <LoadingTable /> : <ListComponent data={data} />}
      </>
    );
  };
}

// الاستخدام:
const StoresListPage = createListPage({
  useDataHook: useStores,
  ListComponent: StoresList,
  pageTitle: 'إدارة المتاجر'
});
```

---

## 📋 خطة التنفيذ الموصى بها

### المرحلة 1: إنشاء البنية التحتية (Infrastructure) - ✅ مكتملة
```
✅ إنشاء المجلدات الأساسية
✅ إنشاء جميع ملفات index.ts
⏳ إعداد بيئة الاختبار (Jest + Testing Library + MSW) - المرحلة القادمة
⏳ إنشاء ملفات __mocks__ - المرحلة القادمة
```

### المرحلة 2: طبقة Services + Utils - ✅ مكتملة
```
✅ إنشاء services/ (6 ملفات)
   - discount.service.ts
   - stores.service.ts
   - products.service.ts
   - offers.service.ts
   - categories.service.ts
   - validation.service.ts
✅ إنشاء formatters/ (4 ملفات)
   - price.formatter.ts
   - date.formatter.ts
   - number.formatter.ts
   - text.formatter.ts
✅ إنشاء transformers/ (4 ملفات)
   - product.transformer.ts
   - offer.transformer.ts
   - store.transformer.ts
   - category.transformer.ts
✅ إنشاء validators/ (3 ملفات)
   - image.validator.ts
   - slug.validator.ts
   - date.validator.ts
⏳ كتابة اختبارات لكل service و util - المرحلة القادمة
```

### المرحلة 3: طبقة Hooks - أسبوع 3
```
✅ إنشاء Form Hooks (useStoreForm, useProductForm...)
✅ إنشاء Utility Hooks (useConfirm, usePagination...)
✅ كتابة اختبارات للـ hooks
```

### المرحلة 4: طبقة Components - أسبوع 4-5
```
✅ إنشاء مكونات stores/
✅ إنشاء مكونات products/
✅ إنشاء مكونات offers/
✅ إنشاء مكونات categories/
✅ كتابة اختبارات للمكونات
```

### المرحلة 5: إعادة هيكلة Pages - أسبوع 6
```
✅ تحويل صفحات stores/
✅ تحويل صفحات products/
✅ تحويل صفحات offers/
✅ تحويل صفحات categories/
✅ اختبارات E2E
```

### المرحلة 6: المراجعة والتحسين - أسبوع 7
```
✅ مراجعة الكود الكامل
✅ تحسين الأداء
✅ توثيق الكود
✅ إزالة الكود القديم
```

---

## 📊 مؤشرات النجاح (Success Metrics)

| المؤشر | قبل | الهدف |
|--------|-----|-------|
| **متوسط حجم ملف الصفحة** | 337 سطر | < 20 سطر |
| **تغطية الاختبارات** | 0% | > 80% |
| **عدد الملفات > 200 سطر** | 12 ملف | 0 ملف |
| **عدد import statements في الملف** | 15+ | < 5 |
| **وقت تحميل الصفحة** | قاعدة | -20% |

---

## 🛠️ الأدوات المطلوبة

```json
{
  "devDependencies": {
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/user-event": "^14.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "msw": "^2.0.0"
  }
}
```

---

## 📞 للمطورين

### قبل البدء:
1. ✅ اقرأ هذا المستند كاملاً
2. ✅ راجع أمثلة الكود في القسم "أمثلة على التدفق"
3. ✅ افهم الفرق بين Hooks و Services
4. ✅ جهّز بيئة الاختبار

### أثناء التطوير:
1. ✅ اتبع مبدأ Single Responsibility
2. ✅ استخدم TypeScript بشكل صارم
3. ✅ اكتب الاختبار قبل أو مع الكود (TDD)
4. ✅ استخدم index.ts في كل مجلد
5. ✅ لا تكرر المنطق بين Hooks و Services

### بعد الانتهاء:
1. ✅ راجع الكود مع فريق آخر
2. ✅ تأكد من تغطية الاختبارات > 80%
3. ✅ احذف الكود القديم
4. ✅ حدّث التوثيق

---

## 📚 مراجع إضافية

- [React Hooks Best Practices](https://react.dev/reference/react)
- [Testing Library Documentation](https://testing-library.com/)
- [Clean Architecture in React](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
- [Separation of Concerns](https://en.wikipedia.org/wiki/Separation_of_concerns)

---

## 📈 التقدم الحالي

**آخر تحديث:** 2025-11-08
**الحالة:** 🚀 قيد التنفيذ - المرحلة 2 مكتملة
**التقدم:** 33% (2/6 مراحل)

### ✅ ما تم إنجازه:
- ✅ المرحلة 1: البنية التحتية (100%)
- ✅ المرحلة 2: Services + Utils (100%)
  - 6 Services
  - 4 Formatters
  - 4 Transformers
  - 3 Validators
  - 25 ملف جديد
  - ~2,500 سطر من الكود

### ⏳ المرحلة القادمة:
- المرحلة 3: Hooks Layer
  - Form Hooks (useStoreForm, useProductForm, useOfferForm, useCategoryForm)
  - Utility Hooks (useDiscountCalculator, usePagination, useConfirm, useFetch)

**المطور المسؤول:** Augment AI Agent
